import React from "react";
import { Drawer } from "antd";

export const DrawerComponent = ({ showDrawer }: DrawerComponentProps) => {
  <Drawer></Drawer>;
};

export interface DrawerComponentProps {
  showDrawer: boolean;
}
